/*import React from 'react';

import './Popup.css';


function PopupModel(props) {
    const handleClose = () => {
        if (props.onClose) {
            props.onClose();
        }
    };

    return props.trigger ? (
        <div className='ED-popup-overlay'>
            <div className='ED-popup'>
            <div className="ED-pop_up_header">
                <button className='ED-close-btn' onClick={handleClose}>x</button>
                </div>
                <div className='ED-popup-inner'>
                    {props.children}
                </div>
            </div>
        </div>
      
    ) : null;
}

export default PopupModel;*/
